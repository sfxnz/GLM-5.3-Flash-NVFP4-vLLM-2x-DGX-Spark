# Grounded hypotheses (read before each attempt) — prose decode tok/s c=1 — 20260903T005500Z

Source: /hypothesis-panel fan-out 1 run 20260902T222621Z (`/tmp/forge-dispatch-glm/evidence/hypotheses-20260902.md`, H1-H3), cross-checked by the GPU worker against `evidence/decision.tsv`, `run.sh` and `hillclimb/NIGHT.md`; H2 (`SPEC=mtp`) added from NIGHT.md's untried list. Nothing was run. Baseline for the night: `evidence/parity-20260902T225109Z/bench.json` (copied to `evidence/rebench-parity-20260903T005500Z/bench.json` so `/hillclimb-night` picks it up): prose c=1 19.23, c=2 15.13 tok/s, acceptance_len 2.29 on prose. Noise 8% (kit/PARITY.md Noise: the 3-run prose ruler spread ±11% across parity runs).

Every candidate below is one `NAME=value` where NAME has a `NAME="${NAME:-...}"` default in `run.sh`, is forwarded to rank 1 on spark2 in the `ssh "$WORKER_HOST"` line (`run.sh:367`; `SPEC_CONFIG` and `COMPILATION_CONFIG` are computed on the head and forwarded too), and passes `VALIDATE_ONLY=1 ./run.sh` (checked 2026-09-03T00:55Z, rc=0 for all four). No `FORCE_UNSAFE_*`.

H1. DFlash2-5 at seqs=2 (drop the two prose-dead draft slots). Mechanism: live DFlash2-7 verifies 8 tokens per step but prose keeps 2.29 (parity `acceptance_len`), so slots 5-7 are almost never accepted and each is a KDA state copy per step. `NUM_SPECULATIVE_TOKENS=5` keeps seqs=2 and derives graphs 1,2,4,6,12. Not h1 (5->6 at seqs=4), not h3 (7@2 vs 5@4): 5@2 has no `decision.tsv` row. In `recipe.yaml serve.env`, so a keep lands as a default. Expected: +1 to +3 prose c=1; structured c=1 falls. Change: `NUM_SPECULATIVE_TOKENS=5 ./run.sh`. Evidence: evidence/parity-20260902T225109Z/bench.json acceptance_len 2.29 / draft_acceptance_rate 0.18 on prose c=1; run.sh:81-89 ladder derivation; evidence/decision.tsv rows h1, h3.

H2. GLM native MTP head instead of the DFlash2 drafter. Mechanism: `SPEC=mtp` switches `--speculative-config` to `{"method":"mtp","num_speculative_tokens":4}` with graphs 1,2,4,8,16,24 (run.sh:68-70, 91). A different drafter with 4 slots: no KDA draft copies, fewer wasted positions on prose. Never measured on this recipe (`decision.tsv` has no `SPEC=` row; README documents it as an option). In `serve.env`. Risk: the checkpoint may lack MTP weights and the boot fails; that is one reverted iteration and a cheap fact. Expected: unknown; the ruler decides. Change: `SPEC=mtp ./run.sh`. Evidence: run.sh:55-70 SPEC switch; hillclimb/NIGHT.md untried list item 2.

H3. One sequence so c=1 never pays the 16-token verify shape. Mechanism: at spec=7 seqs=2 the capture ladder is 1,2,4,8,16; `MAX_NUM_SEQS=1` derives 1,2,4,8 and removes the second-stream KDA copies and scheduler work at c=1. seqs=3 was tried (worse), seqs=1 was not. In `serve.env`. Caveat for the reviewer: at c=2 the second request queues; `bench_decode.py` measures decode from first token, so the c=2 decode gate will not see the queueing (TTFT will). Read `ttft_p50` in the iteration `bench.json` before accepting a keep. Expected: 0 to +2 prose c=1. Change: `MAX_NUM_SEQS=1 ./run.sh`. Evidence: run.sh:81-89; kit/bench_decode.py:98-105 decode window starts at first token; evidence/decision.tsv row h3 (MAX_NUM_SEQS=3 in README).

H4. Drop the vLLM CPU swap-space reservation on UMA. Mechanism: `--swap-space` defaults to 4 GiB of CPU KV per rank; on GB10 that is the same 128 GiB UMA pool as the weights and the fp8 KV pin, and the host already runs into swap during the slow waves. `--swap-space 0` removes the reservation. Skeptics: vLLM V1 may never allocate it, so expected ~0; kept last for that reason. `EXTRA_ARGS` is outside `serve.env`: a keep is recorded, not applied (apply by hand via recipe.yaml). Expected: 0 to +10% prose c=1. Change: `EXTRA_ARGS="--swap-space 0" ./run.sh`. Evidence: run.sh:120 EXTRA_ARGS word-split into the serve command and forwarded to rank 1 at run.sh:367; README.md swap note; evidence/iter-h-snap wave1 18.08 vs wave2 16.92.

Dropped: `NUM_SPECULATIVE_TOKENS=4` (same mechanism as H1; one slot count per night), `EXTRA_ARGS="--async-scheduling"` (may be refused with the DFlash2 backport; keep not auto-applied; lower expected value than H4 for the same cost), `MAX_NUM_BATCHED_TOKENS=1024` (NIGHT.md: probably inside noise at c=1).

Do not stack. One change, measure, keep or revert.
